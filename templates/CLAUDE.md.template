# {{PROJECT_NAME}} - Project Instructions

This file is auto-loaded by Claude Code at the start of every session in this project.
Read it before answering anything.

## Project identity

{{ONE_LINER}}

Project type: {{PROJECT_TYPE}}.

Current local working folder on this machine:

```text
{{CODE_PATH}}
```

If this project is opened from a NAS or synced copy, use the actual current working
directory reported by the shell instead of the path above.

Repository:

```text
{{REPO_URL}}
```

Branch model:

{{BRANCH_MODEL}}

Primary hierarchy:

```text
{{HIERARCHY}}
```

## Brain backend

Canonical memory for this project lives in **{{BRAIN_NAME}}**, at:

```text
{{BRAIN_ROOT}}
```

Start here every session: the re-entry page above states the current state, the next
action, and the active plan. Read it with `{{BRAIN_READ_TOOL}}` rather than guessing at
paths or URLs.

{{SECONDARY_BRAIN_CLAUSE}}

If that backend is unavailable, read `docs/AGENT_BOOTSTRAP.md` as the portable fallback
and tell the user before doing any work that depends on project context.

This choice was made deliberately at project init. Do not switch backends casually; a
switch is a decision to log, not a preference to drift on.

## Hard rules

{{HARD_RULES}}

## Operational defaults

{{OPERATIONAL_DEFAULTS}}

## Before you say "done"

1. Confirm the change matches what the user asked for.
2. Confirm the brain was updated if a unit of work was completed, or state why no
   update was needed.
3. Report briefly: what changed, where in the brain it was logged, what to verify.

## Keep this file and AGENTS.md in sync

`AGENTS.md` carries the same hard rules for non-Claude agents. When a rule changes
here, change it there too. The two files must never drift apart on rules.
